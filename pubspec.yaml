name: berlin_events
description: Berlin Events - Veranstaltungen in Berlin
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.1

flutter:
  uses-material-design: true

  assets:
    - web/events.json
